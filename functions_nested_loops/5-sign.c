#include "main.h"
/**
 * print_sign - prints the sign of a number.
 * @n: char type
 * Return: 1 if is greater than 0, 0 if is zero and -1 if less than 0
 */
int print_sign(int n)
{
	if (n > 0)
		return (1);
			print_sign('+');

			return (0);
}
