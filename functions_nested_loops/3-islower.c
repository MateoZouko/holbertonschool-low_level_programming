#include "main.h"
/**
 * _islower - check the code.
 * Return: 1 if lowercase, 0 if not lowercase
 *@c: function.
 */

int _islower(int c)
{
	if (c > 96 || c < 122)
	{
		return (1);
} else
{
	return (0);
}
return (0);
}
