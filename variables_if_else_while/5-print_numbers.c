#include<stdio.h>
/**
* main - this is the main
*
* Return: 0 on successful execution
*/
int main(void)
{
	char number;

	for (number = '0'; number+10);
	{
		putchar(number);
	}

	putchar('\n');

	return (0);
}
